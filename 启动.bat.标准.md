# 启动.bat 标准规范

## 一、已发现并修复的问题清单

### 编码类
| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | CMD 闪退,菜单中文乱码 | UTF-8 无 BOM 文件被 CMD 以 GBK 读取,多字节序列产生 `\n` `|` `>` 等命令字符 | 文件保存为 **GBK/ANSI 编码**,CMD 原生读取 |
| 2 | `[7] 退出` 被当命令执行 | UTF-8→GBK 误解析导致 `echo` 行被截断,后续行变成裸文本 | GBK 编码 + `echo(` 格式 |
| 3 | 中文常量出现在 `goto` 目标中 | 复制粘贴时误将注释文本填入 `goto` | 所有 `goto` 目标必须是标签名 `:xxx` |

### 文件格式类
| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 4 | CMD 不识别首行 | UTF-8 BOM (`EF BB BF`) 在部分 Windows 版本上导致 `@echo off` 失效 | 无 BOM,文件直接以 `@echo off` 开头 |
| 5 | 行尾解析异常 | Unix LF (`\n`) 换行,CMD 需要 CRLF (`\r\n`) | 全文件 CRLF 换行 |
| 6 | 标签重复导致流程错乱 | 多次编辑时 `:run_server` `:check_mode` 等标签出现 2-8 次 | 每个标签有且仅有 1 次定义 |

### 引擎查找类
| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 7 | 找不到引擎 | 只认 `runtime/bmc-engine.exe` 和 `where python`,不认离线 Python | 增加 4 级 fallback:.venv→offline_bmc_deps→runtime\python311→where python |
| 8 | 硬编码用户目录 | 写死 `C:\Users\xxx\...` 路径 | 全部基于 `%ROOT%` 推导,`%USERPROFILE%` 仅做 fallback 并打印标记 |
| 9 | 源码模式无 PYTHONPATH | 用 Python 启动 run.py 时找不到 `src` 模块 | `if "%USE_PYTHON%"=="1" set "PYTHONPATH=..."` |

### 逻辑流程类
| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 10 | `:check_mode` 缺两路分支 | 删除了 `if HAS_CLI_ARGS goto :run_direct` 和 `goto :menu` | 三路完整:server / direct / menu |
| 11 | `:run_engine` 用 `%*` 丢参数 | `call :子程序` 内 `%*` 指向原始 bat 参数,不是 call 参数 | 改用 `%1 %2 ... %9` 显式传递 |
| 12 | Server 模式不设 PYTHONPATH | 源码模式跑 server 时找不到 `src` 模块 | server 命令前加 `set "PYTHONPATH=..."` |

### 显示类
| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 13 | 菜单行显示 `echo` 命令本身 | `@echo off` 无效或未生效 | 文件以 `@echo off` 开头,无 BOM |
| 14 | 全角括号引发误解析 | `（` `）` `，` 等 UTF-8 多字节被 GBK 误读产生命令字符 | 全部替换为半角 `()` `,` |
| 15 | 错误提示暗示需安装 Python | `请安装 Python 3.9+` 误导用户 | 改为列出已检查的路径,+Unblock-File 提示 |

### 安全类
| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 16 | Windows "无法验证发布者" | exe 未签名或下载文件被加安全标记 | 错误提示 + README 中加 `Unblock-File` 说明 |

---

## 二、编写规范(后续必须遵守)

### 2.1 文件编码
- **编码**:GBK/ANSI (中文 Windows CMD 原生编码)
- **BOM**:无
- **首行**:纯 ASCII `@echo off\r\n`
- **换行**:CRLF (`\r\n`)

### 2.2 显示输出
- 所有菜单行使用 `echo(` 而非 `echo`
- 空白行用 `echo.` 而非 `echo(` 或 `echo`
- 全角括号 `（）` 一律改为半角 `()`
- 全角逗号 `，` 一律改为半角 `,`
- 显示 `%` 时使用 `%%` 转义

### 2.3 路径定位
```bat
set "ROOT=%~dp0"
if "%ROOT:~-1%"=="\" set "ROOT=%ROOT:~0,-1%"
cd /d "%ROOT%"
```
所有路径从 `%ROOT%` 推导,禁止写死 `C:\Users\xxx`。

### 2.4 引擎查找优先级
1. `%ROOT%\runtime\bmc-engine.exe`
2. `%ROOT%\.venv\Scripts\python.exe` + `run.py`
3. `%ROOT%\offline_bmc_deps\python311\python.exe` + `run.py`
4. `%ROOT%\runtime\python311\python.exe` + `run.py`
5. `%USERPROFILE%\...` (fallback,需打印 `[fallback]` 标记)
6. `where python` (最后兜底)

### 2.5 标签使用
- 每个标签定义 `:label` 必须唯一
- `goto :label` 目标必须是已有标签,禁止中文文本
- 标签不能包含空格或特殊字符

### 2.6 子程序参数
- `call :subroutine arg1 arg2` 内用 `%1 %2` 取值
- **禁止**在子程序内用 `%*`,它始终指向 bat 原始参数
- 需要传 9 个以上参数时改用变量传递

### 2.7 PYTHONPATH 设置
- 使用 Python 模式时必须设置 `PYTHONPATH`
- 最少包含: `%ROOT%\app;%ROOT%`
- Server 模式在引擎执行前必须设置

### 2.8 检查清单(提交前逐项核对)
- [ ] 编码:GBK 可解码,所有行有效
- [ ] 换行:CRLF,无裸 LF
- [ ] 首行:`@echo off\r\n`
- [ ] 无 `chcp 65001`
- [ ] 无 UTF-8 BOM
- [ ] 所有标签唯一 (定义 `:label`,不计 `goto :label`)
- [ ] 无中文 goto 目标
- [ ] 所有菜单行以 `echo(` 开头
- [ ] 无旧式 `echo ` (不含 `echo.` 和 `echo(`)
- [ ] 无全角括号 `（）` `，`
- [ ] `:check_mode` 三路完整
- [ ] `:run_engine` 用 `%1-%9` 不用 `%*`
- [ ] PYTHONPATH 在 server 命令前设置
- [ ] 无硬编码用户目录
- [ ] 离线 Python 路径基于 `%ROOT%`
- [ ] `[fallback]` 标记在 USERPROFILE 路径前
- [ ] 错误提示不含"需自行安装 Python"
- [ ] Unblock-File 提示存在
- [ ] 36 项自动化验证全部通过
